{
  "name": "countdown-app-root",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "shopify",
    "build": "shopify app build",
    "dev": "shopify app dev --skip-dependencies-installation",
    "info": "shopify app info",
    "generate": "shopify app generate",
    "deploy": "shopify app deploy"
  },
  "dependencies": {
    "@countdown/frontend": "*",
    "@countdown/backend": "*"
  },
  "devDependencies": {
    "@shopify/app": "3.51.2",
    "@shopify/cli": "3.51.2"
  }
}